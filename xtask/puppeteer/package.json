{
  "name": "bee-viz-snapshot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless-Chrome screenshot driver for bee's WASM visual scenes. Invoked by `cargo xtask viz-snapshot`.",
  "scripts": {
    "snapshot": "node snapshot.mjs"
  },
  "dependencies": {
    "pixelmatch": "^7.1.0",
    "pngjs": "^7.0.0",
    "puppeteer": "^24.10.0"
  }
}
