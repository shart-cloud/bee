# Threat Model: bee

## 1. System context

bee is a Rust workspace that gives AI coding-agent tools capability-scoped access to a Linux host. Its security-critical path parses declarative policy, proves child-policy attenuation, resolves host identities and destinations, builds an eBPF-specific `EnforcementPlan`, attaches BPF LSM programs, creates a cgroup per scope, and enforces file-open, executable, and IP-connect decisions in the kernel.

The workspace also includes a harness for prompts, external model providers, built-in shell/file tools, local and remote MCP servers, transcripts and audit, CTF evaluation, in-process rendering, and work-in-progress `SKILL.md` discovery and capability grants. Intended users are agent operators, framework integrators, and evaluation researchers. Production enforcement requires Linux BPF LSM/BTF/cgroup v2 and a privileged loader; the default harness build is process-hardened and credential-stripped but not kernel-confined. This analysis covers the current dirty worktree, including uncommitted skill support.

The intended authority model is a protected, user-owned bee configuration: absent an explicit grant, access is denied; users may deliberately grant broad authority; and agents or repository content may request but cannot silently widen that authority. Launch-configured or directly invoked skills are explicit instruction choices, while capability changes require HITL approval and remain bounded by attenuation. MCP servers also require explicit configuration: bee-spawned servers belong inside the enforced scope, while pre-existing local or remote servers are outside bee's semantic control and bee owns only connection, credential, tool-visibility, and consequence-containment boundaries.

## 2. Assets

| asset | description | sensitivity |
|---|---|---|
| A1 — Provider API keys, MCP bearer tokens, and operator credentials | External-service authority, billing credentials, SSH/cloud secrets, and arbitrary sensitive files on the host. | critical |
| A2 — Host, harness process, and kernel enforcement integrity | Privileged orchestrator memory/environment, BPF programs/maps, cgroup membership, active LSM links, and surrounding host processes. | critical |
| A3 — Project source, artifacts, VCS metadata, and bee control-plane files | Real work product plus policy, scenario, provider, MCP, and skill configuration that determines future behavior and authority. | critical |
| A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions | The authoritative definition of allowed file, process, network, and tool capabilities. | critical |
| A5 — Network egress authority and downstream services | Model APIs, registries, remote MCP, internal endpoints, service mutations, and billable usage reachable from the host or scoped child. | critical |
| A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | Potentially proprietary or secret interaction content and the evidence used to verify confinement. | high |
| A7 — Harness availability and resource budget | CPU, memory, disk, audit capacity, provider tokens/cost, terminal state, and ability to launch confined work. | high |
| A8 — CTF flags and evaluation/scoring integrity | Ground truth for adversarial evaluation and reproducible research results. | high |

## 3. Entry points & trust boundaries

| entry_point | description | trust_boundary | reachable_assets |
|---|---|---|---|
| E1 — CLI, environment, and host-side config/scenario materialization | CLI flags and TOML/YAML paths choose policies, parents, cgroup root, commands, workdirs/files, providers, endpoints, tokens, tools, skills, limits, and output paths; scenario files/directories are created before sandbox construction. | Operator or repository-controlled input → privileged host process and filesystem | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E2 — External model-provider API | The harness sends prompts, tool schemas/results, and history to Anthropic or an OpenAI-compatible base URL; responses select tool names and arguments. | Local sensitive context and credential → external provider → tool dispatcher | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E3 — Built-in shell, process, and file tools | Model-controlled JSON invokes `sh -c`, `cat`, `tee`, `ls`, and other allowed binaries through either an enforced cgroup scope or host-only hardened mode. | Untrusted model output → sandboxed child or launcher-user host authority | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E4 — MCP stdio servers | Config selects an arbitrary executable, arguments, and environment; the server speaks JSON-RPC and dynamically contributes tools. It is cgroup-scoped only in enforce builds. | MCP config and third-party child process ↔ harness registry/model conversation | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget |
| E5 — Remote MCP Streamable HTTP | A host-side client validates the authored hostname then connects using an optional bearer token; schemas, results, and list changes alter model-visible tools. | Trusted host credentials/network ↔ remote server-controlled content and behavior | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget |
| E6 — Project/user skill discovery and invocation | Host-side code scans roots, parses YAML frontmatter, injects descriptions/bodies verbatim, applies project-over-user shadowing, and parses tool/filesystem grant requests. | Repository/user filesystem instructions → trusted orchestrator and model behavior | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E7 — In-process Rhai render tool | Model-authored script is evaluated inside the harness process under explicit Rhai resource limits and a restricted drawing API. | Untrusted model script → trusted harness address space | A2 — Host, harness process, and kernel enforcement integrity, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget |
| E8 — Policy compilation, ambient resolution, attenuation, and planning | Policy input combines with cwd/HOME/PATH/DNS, protected defaults, parent-child subset logic, and fixed backend capabilities to produce map-ready rules. | Declarative authority and ambient host state → kernel-installable policy | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A7 — Harness availability and resource budget |
| E9 — Privileged loader, cgroup lifecycle, and BPF map installation | The host attaches global LSM hooks, creates scope cgroups, inserts map entries, moves children during `pre_exec`, consumes the audit map, and tears down cgroup directories. | Privileged userspace → global kernel hooks and per-scope enforcement state | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget |
| E10 — eBPF LSM hooks | `file_open`, `bprm_check_security`, and `socket_connect` resolve runtime objects, consult cgroup-keyed maps, allow or deny, and reserve audit records. | Sandboxed process syscall → kernel policy decision and audit stream | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E11 — Audit, transcript, metrics, and output persistence | Kernel records and model/tool exchanges are buffered, demultiplexed, correlated, serialized, printed, and written to operator-selected files. | Security-sensitive runtime data → host memory/stdout/filesystem and downstream consumers | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity |
| E12 — Cargo/toolchain, GitHub Actions, and privileged VM test pipeline | Third-party actions, crates, rolling toolchains, `bpf-linker`, locally built artifacts, SSH tunnels, and sudo test runners build or validate a privileged security product. | External supply chain and remote test infrastructure → privileged release artifacts and VM root | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget |

## 4. Threats

| id | threat | actor | surface | asset | impact | likelihood | status | controls | evidence |
|---|---|---|---|---|---|---|---|---|---|
| T1 | An agent or child process turns the trusted harness into a confused deputy that exercises root/kernel, host-filesystem, or secret-backed network authority beyond the caller's effective policy. | remote_auth | E3 — Built-in shell, process, and file tools | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services | critical | likely | unmitigated | Child hardening, attenuation primitives, privileged-target refusal, output/time limits, and credential-name stripping; protected user authority plus end-to-end HITL widening is not fully implemented. | |
| T2 | A malicious privileged child escapes or disables confinement by retaining root/capabilities, migrating out of its cgroup, signaling the loader, or manipulating BPF/cgroup state. | supply_chain | E9 — Privileged loader, cgroup lifecycle, and BPF map installation | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions | critical | likely | unmitigated | Setuid/file-capability targets are refused; core dumps, dumpability, and `LD_*` are hardened; `pre_exec` joins the cgroup. No uid/gid/capability drop or `no_new_privs` boundary is visible. | |
| T3 | An insider creates divergence between policy intent and actual enforcement, yielding false assurance, unintended authority, or fail-late outages. | insider | E8 — Policy compilation, ambient resolution, attenuation, and planning | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services | critical | likely | partially_mitigated | Two-stage `CompiledPolicy` → `EnforcementPlan`; unsupported capabilities/capacity reject before kernel mutation; attenuation, planner, CLI, and VM tests. | `.scratch/truthful-enforcement-plan/issues/01-make-compiled-policy-runnable.md`; ca7a240 |
| T4 | A malicious workload uses operations outside the attached LSM hooks or induces helper error paths to bypass intended file, exec, or egress restrictions. | supply_chain | E10 — eBPF LSM hooks | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A5 — Network egress authority and downstream services | critical | likely | unmitigated | Kernel-resolved paths, cgroup-keyed maps, support/offset gates, and three LSM hooks; rename/link/unlink, inherited descriptors, UDP `sendto`, non-IP sockets, path aliases, and several helper failures remain outside or allow. | |
| T5 | A trojan repository uses scenario/provider configuration to make the trusted parent write host files or send an arbitrarily selected environment secret to an attacker-controlled provider before sandboxing. | supply_chain | E1 — CLI, environment, and host-side config/scenario materialization | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | critical | likely | unmitigated | Typed parsing and some semantic validation; workdir materialization remains host-side and `base_url`/`api_key_env` are config-controlled. Protected user authority and mandatory HITL for repository-originated changes are not wired end-to-end. | |
| T10 | A malicious workload exploits filesystem behavior that diverges from the intended strict user-configured default-deny model to expose or modify host/project data. | supply_chain | E10 — eBPF LSM hooks | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | critical | likely | unmitigated | Protected defaults, explicit access modes, write-default flag, and observe mode exist; unmatched reads, conditional writes, and path aliases do not yet implement strict deny-unless-user-granted semantics. | |
| T6 | Bee's remote MCP connection handling reaches an unintended destination or discloses a bearer token through URL, redirect, destination-gating, or credential-handling mistakes. | remote_auth | E5 — Remote MCP Streamable HTTP | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | critical | possible | partially_mitigated | Remote MCP requires explicit configuration, defaults disabled, empty allowlist denies, deny precedence, TLS defaults, timeouts, and server caps. Redirect and connection revalidation are not evident. | |
| T7 | A compromised bee-spawned MCP stdio binary escapes equivalent sandboxing or receives reintroduced secrets, gaining authority beyond its configured scope. | supply_chain | E4 — MCP stdio servers | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | critical | possible | partially_mitigated | Enforce builds use the same cgroup-scoped child path as built-in tools and strip named credentials. Host mode remains unconfined and explicit config env is added after command construction. | |
| T8 | A supply-chain or network attacker substitutes a privileged userspace/BPF artifact through mutable crates, actions, toolchains, `bpf-linker`, or a MITM'd VM test endpoint. | supply_chain | E12 — Cargo/toolchain, GitHub Actions, and privileged VM test pipeline | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A2 — Host, harness process, and kernel enforcement integrity, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions | critical | possible | unmitigated | Local dependency resolution, code review, host checks, the kernel verifier, and a VM matrix; immutable/reproducible release inputs and mandatory live enforcement gates are future work. | |
| T9 | A trojan repository's project skill injects trusted-looking instructions that steer tool use, solicit secrets, or induce approval of capability grants without a valid explicit invocation. | supply_chain | E6 — Project/user skill discovery and invocation | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | high | likely | unmitigated | Invocation flags, deterministic precedence, capability ceiling, consent abstraction, and non-interactive `DenyAll`; launch/direct-invocation trust and HITL policy widening are not fully wired end-to-end. | |
| T11 | A provider operator, compromised provider account, or malicious compatible endpoint discloses prompts/source/tool output/flags, returns adversarial tool calls, or consumes excessive billable capacity. | remote_auth | E2 — External model-provider API | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A5 — Network egress authority and downstream services, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity | high | possible | unmitigated | TLS/provider library, turn/time/token settings, retry classification, tool registry, and child credential stripping; reviewed starter allowlists and HITL-reviewed denied-DNS suggestions are not implemented. | |
| T12 | A local attacker replaces or redirects an executable/path identity after policy preparation so different code or files satisfy the reviewed path-based rule. | local_user | E8 — Policy compilation, ambient resolution, attenuation, and planning | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions | high | possible | unmitigated | Names resolve before planning; failures reject; LSM resolves actual paths; inode-pin requests reject rather than weaken. Stable executable identity is not implemented. | |
| T13 | Scope churn or induced failures leave partial/stale cgroup-keyed BPF state that misapplies old rules after ID reuse or exhausts map capacity. | local_user | E9 — Privileged loader, cgroup lifecycle, and BPF map installation | A2 — Host, harness process, and kernel enforcement integrity, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A7 — Harness availability and resource budget | high | possible | unmitigated | Planning precedes kernel mutation and teardown removes the cgroup directory; no transactional rollback or removal of `SCOPES`/FS/EXEC/NET entries is visible. | |
| T14 | A local actor reads or tampers with transcripts, metrics, audit targets, or operator-selected output files, exposing secrets and undermining evaluation evidence. | local_user | E11 — Audit, transcript, metrics, and output persistence | A1 — Provider API keys, MCP bearer tokens, and operator credentials, A3 — Project source, artifacts, VCS metadata, and bee control-plane files, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A8 — CTF flags and evaluation/scoring integrity | high | possible | risk_accepted | Per-tool truncation and structured serialization; the operator owns persistence and protection. Restrictive owner-only creation and redaction are deferred. | |
| T15 | A noisy or malicious workload overflows the audit ring or unbounded subscriber queues, losing denial evidence or exhausting harness memory. | supply_chain | E11 — Audit, transcript, metrics, and output persistence | A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget, A8 — CTF flags and evaluation/scoring integrity | high | possible | unmitigated | Fixed-size kernel ring, cgroup demultiplexing, and per-call drain/re-filtering; ring reservation failure drops records and subscriber channels are unbounded. | |
| T17 | A contributor introduces an enforcement regression that ships because CI treats the enforce check as best-effort and leaves live BPF-LSM behavior to a manual external-VM gate. | insider | E12 — Cargo/toolchain, GitHub Actions, and privileged VM test pipeline | A2 — Host, harness process, and kernel enforcement integrity, A4 — Sandbox policy, attenuation ceiling, executable identity, and consent decisions, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence | high | possible | unmitigated | Host unit/property/integration tests and a documented VM matrix; enforce CI is `continue-on-error` and does not run the live matrix. | |
| T16 | An untrusted model exploits an interpreter/API defect or pathological computation in the in-process Rhai render tool to degrade the harness. | remote_auth | E7 — In-process Rhai render tool | A2 — Host, harness process, and kernel enforcement integrity, A6 — Prompts, model responses, tool data, skills, transcripts, metrics, and audit evidence, A7 — Harness availability and resource budget | medium | possible | risk_accepted | Opt-in tool, restricted API, `eval` disabled, and operation/call/expression/string/collection/render caps; the owner accepts the trusted-interpreter boundary for untrusted visualization scripts. | |

## 5. Deprioritized

| threat | reason |
|---|---|
| Database injection and record-level authorization failures | No database, ORM, or query interface exists in the reviewed repository. |
| Browser-origin CSRF, XSS, and session fixation | No browser-served application or cookie-authenticated web UI is present. |
| Unauthenticated inbound service takeover | bee is primarily a local CLI/library/harness; outbound model and MCP connections are covered instead. |
| Kubernetes/IAM lateral identity and residual cloud grants | No Kubernetes, Terraform, cloud IAM, service-account, or dataset ACL configuration is managed in this tree; privileged local and VM identities are covered under E9/E12. |
| Multi-user repudiation | The repository models a local operator and scoped workloads rather than mutually distrusting application users. Audit integrity and completeness are covered separately. |
| Remote or pre-existing MCP server semantics | Out of scope by owner decision: explicit configuration means the operator trusts the server's behavior. Bee remains responsible for connection, credential, tool-visibility, and consequence-containment controls. |
| DNS record changes and domain takeover | Out of scope by owner decision; users own DNS freshness and flushing. |
| T14 — Transcript confidentiality at rest | Risk accepted: records live on the user/server and the user is responsible for preserving and protecting them; restrictive creation may be added later. |
| T16 — In-process Rhai rendering | Risk accepted: the restricted Rhai interpreter is an intentional boundary for untrusted visualization scripts. |

## 6. Open questions

- [Owner-states] Users may run bee against untrusted or trojan repositories. Affects: T5 actor/likelihood, T9 actor/likelihood. Verify by: document this deployment assumption in the product security contract and test repository-originated config/skill flows.
- [Owner-states] The intended filesystem model denies every read/write absent an explicit grant from protected user configuration. Affects: T4 impact/likelihood/status, T10 impact/likelihood/status. Verify by: end-to-end policy tests for unmatched reads/writes and every supported filesystem operation.
- [Owner-states] Agents cannot modify the authority configuration except through HITL escalation. Affects: T1 status, T5 status, T9 status. Verify by: integration tests proving direct writes and non-HITL policy mutations fail.
- [Owner-states] A project skill is trusted only when selected in launch configuration or directly invoked; every capability widening still requires HITL and attenuation. Affects: T9 controls/status. Verify by: launch, dynamic invocation, shadowing, and grant-path tests.
- [Owner-states] Bee-spawned MCP servers must receive equivalent enforcement; pre-existing local/remote servers require explicit configuration and their semantics are outside scope. Affects: T6/T7 threat boundary. Verify by: cgroup-membership tests and documented external-server trust semantics.
- [Owner-states] Denied DNS destinations may become review suggestions but must never mutate allowlists automatically. Affects: T11 controls/status. Verify by: policy-update tests requiring an authenticated HITL decision.
- [Owner-states] Any detected audit loss invalidates an episode's enforcement evidence. Affects: T15 impact/status. Verify by: kernel/userspace drop accounting and episode-status tests.
- [Owner-states] Enforcement compilation and the live BPF-LSM matrix must become mandatory release gates. Affects: T8/T17 status and recommended mitigation. Verify by: protected-branch CI configuration and a failing-gate rehearsal.
- [Owner-states] Transcript protection at rest is currently the operator's responsibility. Affects: T14 status. Verify by: operator documentation and a later decision on default file permissions.
- [Owner-states] In-process restricted Rhai is an accepted boundary for untrusted visualization scripts. Affects: T16 impact/status. Verify by: continued resource-limit and API-surface regression tests.
- Production child identity remains unknown: the code does not visibly drop uid/gid, supplementary groups, or capabilities before exec. Affects: T2 likelihood/status. Verify by: inspect deployment wrapper and record runtime credentials/capability sets in the VM matrix.

## 7. Provenance

- mode: bootstrap-then-interview
- date: 2026-07-21
- target: /home/jg/git/bee @ e4152c5 (dirty worktree)
- inputs: local source/specs/docs + git log + local issue export + public GitHub repository advisory endpoint
- owner: present, unnamed

## 8. Recommended mitigations

| mitigation | threat_ids | closes_class | effort |
|---|---|---|---|
| Put all elevated operations behind a privilege-separated enforcement broker that accepts only effective-policy-checked, HITL-approved requests. | T1,T2,T5 | yes | L |
| Refuse untrusted model execution unless kernel enforcement is compiled and active; require an explicit, loudly recorded unsafe-host-mode opt-in. | T1,T7 | yes | S |
| Require an executable policy-conformance matrix for every documented capability/mode and gate releases on the live kernel matrix. | T3,T17 | partial | M |
| Enforce complete mediation: default-deny every filesystem/process/network operation and grant only capabilities explicitly present in protected user configuration. | T4,T10 | yes | L |
| Make user authority configuration agent-immutable; repository content may request changes, but only explicit invocation plus HITL can alter effective authority. | T5,T9 | yes | M |
| Apply transport-aware MCP containment: sandbox spawned servers and gate external destinations, credentials, and tool visibility after explicit configuration. | T6,T7 | partial | M |
| Commit lockfiles, pin actions/toolchains/bpf-linker immutably, verify artifacts, enable SSH host verification, and make live BPF tests mandatory. | T8,T17 | partial | L |
| Ship reviewed provider/network starter allowlists and turn denied DNS observations into HITL review suggestions, never automatic grants. | T11 | partial | M |
| Bind executable and sensitive file rules to stable identities rather than mutable path names. | T12 | partial | L |
| Make scope creation transactional, roll back failures, and remove every per-cgroup map entry before cgroup teardown; test ID reuse and churn. | T13 | yes | M |
| Add bounded audit queues and kernel/userspace drop accounting that automatically marks episode enforcement evidence incomplete. | T15 | yes | M |
